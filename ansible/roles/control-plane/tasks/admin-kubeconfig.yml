---
# Create admin kubeconfig for cluster management

- name: Create admin kubeconfig
  ansible.builtin.template:
    src: admin.kubeconfig.j2
    dest: "{{ k8s_config_dir }}/admin.kubeconfig"
    mode: '0600'
  become: true
