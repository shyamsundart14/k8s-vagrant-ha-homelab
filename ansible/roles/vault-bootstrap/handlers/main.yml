---
# Vault handlers

- name: reload systemd
  ansible.builtin.systemd:
    daemon_reload: yes
  become: true

- name: restart vault
  ansible.builtin.systemd:
    name: vault
    state: restarted
  become: true
